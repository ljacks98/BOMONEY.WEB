Put cover art here (e.g. better-days-ahead.jpg, snakes-cover.jpg).

The site currently uses plain color-gradient covers instead of real images.
To use a real image for a release or beat, edit `js/main.js` and add an
`image: "assets/images/your-file.jpg"` field to that entry, then update the
render code to use it — ask Claude for a hand with that wiring if needed.
